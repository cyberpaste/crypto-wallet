{% extends "base.twig" %}
{% block content %}  
    <header class="masthead bg-primary text-white text-center">
        <div class="container">
            <h1 class="text-uppercase mb-0 mx-aut" >404</h1>
            {% if app.session.getFlash('error') %}
                <h3>
                    {{app.session.getFlash('error')}}
                </h3>
            {% endif %}
        </div>
    </header>
{% endblock %}